{
  "name": "streaming-wcp-agent",
  "version": "0.1.0",
  "description": "Per-server deployment agent. Exposes deploy/update/delete/alias/cleanup over an authenticated HTTP API with live SSE logs.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}